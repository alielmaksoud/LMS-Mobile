import { StyleSheet } from "react-native";

export default StyleSheet.create({
 
  selectors: {
    flexDirection: "row",
    height: 50,
    paddingLeft: 20,
    marginBottom: 10,
    flex:1
  },
});
